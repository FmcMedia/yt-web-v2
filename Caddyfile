https://192.168.1.2 {
    tls internal

    # Serve Caddy root CA cert for Android installation
    handle /caddy-root.crt {
        root * /data/caddy/pki/authorities/local
        rewrite * /root.crt
        file_server
    }

    reverse_proxy yt-web:8000
}
