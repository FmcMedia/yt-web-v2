services:
  yt-web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./downloads:/app/downloads
      - ./transcripts:/app/transcripts
    environment:
      - STORAGE_LIMIT_GB=20
    restart: unless-stopped
